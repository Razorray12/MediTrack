<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.HospitalPatientInformationReferenceSystem" parent="Theme.Material3.DayNight.NoActionBar"></style>

    <style name="Theme.HospitalPatientInformationReferenceSystem" parent="Base.Theme.HospitalPatientInformationReferenceSystem">
        <item name="android:statusBarColor">@color/main</item>
        <item name="colorControlActivated">@color/handles</item>
        <item name="android:textColorHighlight">@color/highlights</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="MyMenuItemStyle" parent="ShapeAppearanceOverlay.Material3.NavigationView.Item">
        <item name="colorControlHighlight">@color/handles</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MediTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MediTrack" parent="Base.Theme.MediTrack" />


</resources>
